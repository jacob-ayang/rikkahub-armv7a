# ARM V7 Build Configuration
# Set this property to build for ARM V7 architecture only

# When gradle.properties contains this, the app will build for ARM V7 only
# Usage: ./gradlew assembleRelease -Dbuild.armv7=true
build.armv7=false
